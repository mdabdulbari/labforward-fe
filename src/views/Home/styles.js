const styles = (theme) => ({
    container: {
        width: "50%",
        margin: "auto",
        marginTop: 50,
    },
    input: {
        marginBottom: 20,
    },
    button: {
        margin: "10px 5px",
    },
});

export default styles;
